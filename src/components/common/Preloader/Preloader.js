import React from 'react';
import preloader from '../../../../src/reloader.gif';


const Preloader = (props) => {
    return <div>
        <img src={preloader} />
    </div>
}

export default Preloader